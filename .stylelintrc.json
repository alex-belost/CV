{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "import", "plugin", "source", "utility", "variant", "custom-variant"]
      }
    ],
    "import-notation": "string" 
  }
}
